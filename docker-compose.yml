services:
  web:
    build:
      context: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      DEBUG: 'True'
      HUGGING_FACE_HUB_TOKEN: ${HUGGINGFACE_TOKEN}
    env_file:
      - .env
